[project]
name = "sentinel-services"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[tool.uv.workspace]
members = [
    "api",
    "dia",
    "pub",
    "diagnosis",
    "persistence",
]
